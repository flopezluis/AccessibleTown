{% extends "layouts/app.html" %}
{% block jquery_init %}
{% load i18n %}
//window load is called when all images has been loaded
// then apply shadows with all elements on screen
$(window).load(function() {
        $(".whiteboard").dropShadow({left: 0, top: 1, blur: 2, opacity: 0.2} )
});
$("#id_submit").click(function() {
    $("form").submit();
});
{% endblock %}

{% block page %}
<div class="span-24 last">
    <div id="circles" class="span-3 append-14 last">
        <img src="/media/ui/img/circles_top_left.png" alt="" />
    </div>

    <div class="whiteboard prepend-1 span-22 last" style="padding:16px;">

    <h2>{% trans "Restablecer la contraseña" %}</h2>

    <p>{% trans "¿Olvidaste la contraseña? Introduce tu e-mail abajo y te enviaremos un enlace para que puedas restablecerla." %}</p>

    <form action="" method="post">
        {% if form.email.errors %}
        <div class="error">
            {{ form.email.errors.0 }}
        </div>
        {% endif %}        
        <label for="id_email">email:&nbsp;</label> {{ form.email }}
        <p class="help_text">{% trans "Asegúrate de que el mail es el mismo que usaste para registrarte." %}</p>
        <a id="id_submit" href="#" class="reset_pass_btn">{% trans "Restablecer contraseña" %} </a>

    </form>
</div>
</div> <!-- login_container -->  
{% endblock %}
